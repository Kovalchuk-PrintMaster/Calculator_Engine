
Константи (несекретні)

DEFAULT_CURRENCY = "UAH" — базова валюта відображення.

SUPPORTED_AUDIENCES = ("b2c", "b2b", "partner") — допустимі аудиторії у валідації API.

Секретів у константах не зберігаємо — тільки .env/секрет-менеджер.
