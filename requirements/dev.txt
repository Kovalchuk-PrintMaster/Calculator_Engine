# =============================================================================
# Development dependencies (Calculator Engine)
# -----------------------------------------------------------------------------
# This file extends runtime deps from requirements/app.txt with tools for
# testing, linting, formatting, and local HTTP testing.
# Usage:
#   pip install -r requirements/dev.txt
# =============================================================================

-r app.txt
# Include all runtime dependencies from app.txt.
# Why: a dev environment should mirror production runtime libs
# so tests run against the same stack (FastAPI, SQLAlchemy, etc.).

pytest>=8.0.0
# Test runner & assertion framework.
# Why: simple asserts, fixtures, rich reporting; ecosystem-friendly (plugins).
# Notes:
# - Run tests: `pytest -q`
# - Filter by keyword: `pytest -k "health"`

black>=24.0.0
# Opinionated code formatter.
# Why: auto-formats code to a consistent style, reduces code review noise.
# Notes:
# - Run: `black .`
# - Line length configured in pyproject.toml (100 chars).

ruff>=0.5.0
# Extremely fast linter and import sorter.
# Why: combines multiple linters (pyflakes, pycodestyle, isort, bugbear, pyupgrade).
# Notes:
# - Run: `ruff check .` and autofix: `ruff check . --fix`
# - Config in pyproject.toml (rules: E,F,I,B,UP).

httpx>=0.27.0
# HTTP client used by Starlette/FastAPI TestClient under the hood.
# Why: required for `from fastapi.testclient import TestClient` in unit tests.
# Notes:
# - Without httpx, importing TestClient will raise a RuntimeError.
# - We use TestClient for endpoint integration tests (in tests/).
# --- Static type checking ---
mypy>=1.10.0       # Static type checker for Python; helps catch bugs pre-runtime.

# Optional plugin: improves Pydantic types inference (safe to enable now, even if used lightly).
pydantic>=2.7      # already in app.txt via -r, but listed here to keep plugin compatibility clear
# --- Alembic (міграції БД) ---
alembic>=1.14.0
